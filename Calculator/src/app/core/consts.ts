export enum Methods {
  add,
  subtract,
  multiply,
  divide,
  sqRoot
}

export enum Buttons {
  short,
  tall,
  smallFont,
  guards,
  warn
}

export const ErrorMessages = {
  MemoryEmpty: 'Memory Empty',
  Error: 'Error'
}
